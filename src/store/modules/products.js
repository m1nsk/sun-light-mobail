// initial state
const state = {
  productCode: ''
}

// getters
const getters = {
  productCode: state => state.productCode
}

// actions
const actions = {
  //
}

// mutations
const mutations = {
  //
}

export default {
  state,
  getters,
  actions,
  mutations
}
